backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)


media_folder: "static/img"
publci_folder: "img"

collections:
  - name: "page"
    label: "pages"
    files:
      - file: "site/content/_index.md"
        lavel: "Home Page"
        name: "home"
        fields:
          - {label: "Banner Heading", name: "title", widget: "string"}

